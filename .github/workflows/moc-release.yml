name: MoC-Release
on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Version bump type (auto = detect from commits)'
        required: false
        default: 'auto'
        type: choice
        options:
          - auto
          - patch
          - minor
          - major

permissions:
  contents: write

concurrency:
  group: version-bump
  cancel-in-progress: false

jobs:
  check_branch:
    name: Verify branch
    runs-on: ubuntu-22.04
    steps:
      - name: Ensure running on master
        run: |
          if [ "${{ github.ref_name }}" != "master" ]; then
            echo "::error::MoC-Release publishes to production (dapp.moneyonchain.com) and must only run from 'master' (got '${{ github.ref_name }}'). Refusing to continue."
            exit 1
          fi

  bump_version:
    name: Bump Version
    needs: check_branch
    uses: ./.github/workflows/bump-version.yml
    with:
      bump_type: ${{ inputs.bump_type || 'auto' }}

  create_release:
    name: Create Release
    runs-on: ubuntu-22.04
    needs: bump_version
    if: ${{ needs.bump_version.result == 'success' && needs.bump_version.outputs.new_tag != '' }}
    permissions:
      contents: write
    env:
      REACT_APP_RLOGIN_WALLETCONNECT2_PROJECTID: ${{ secrets.REACT_APP_RLOGIN_WALLETCONNECT2_PROJECTID }}
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: setup Node
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 14.x
          registry-url: 'https://npm.pkg.github.com'

      - name: Install setup
        run: npm ci

      # Target Mainnet
      - name: Build setup Mainnet
        run: CI=false npm run build:moc-mainnet

      - name: Pin to IPFS Mainnet
        id: upload
        env:
          PINATA_API_KEY: ${{ secrets.PINATA_API_KEY }}
          PINATA_SECRET_KEY: ${{ secrets.PINATA_API_SECRET_KEY }}
          PIN_NAME: MoC Mainnet ${{ needs.bump_version.outputs.new_tag }}
        run: |
          set -e
          FORM_ARGS=()
          mapfile -t DIST_FILES < <(find ./build -type f | sort)
          if [ ${#DIST_FILES[@]} -eq 0 ]; then
            echo "::error::No files found in ./build — build may have failed"
            exit 1
          fi
          for FILE in "${DIST_FILES[@]}"; do
            REL="${FILE#./}"
            FORM_ARGS+=(-F "file=@${FILE};filename=${REL}")
          done
          FORM_ARGS+=(-F "pinataMetadata={\"name\":\"$PIN_NAME\"}")

          if ! RESPONSE=$(curl -s --fail-with-body -X POST "https://api.pinata.cloud/pinning/pinFileToIPFS" \
            -H "pinata_api_key: $PINATA_API_KEY" \
            -H "pinata_secret_api_key: $PINATA_SECRET_KEY" \
            "${FORM_ARGS[@]}"); then
            echo "::error::Pinata upload failed: $RESPONSE"
            exit 1
          fi

          HASH=$(echo "$RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin)['IpfsHash'])")
          if [ -z "$HASH" ]; then
            echo "::error::Pinata returned unexpected response: $RESPONSE"
            exit 1
          fi
          echo "hash=$HASH" >> "$GITHUB_OUTPUT"

      - name: Convert CIDv0 to CIDv1 Mainnet
        id: convert_cidv0
        env:
          CIDV0: ${{ steps.upload.outputs.hash }}
        run: |
          set -e
          CIDV1=$(python3 << 'PYEOF'
          import base64, os
          def b58decode(s):
              alph = '123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz'
              n = 0
              for c in s: n = n * 58 + alph.index(c)
              size = (n.bit_length() + 7) // 8
              return n.to_bytes(max(size, 1), 'big')
          mh = b58decode(os.environ['CIDV0'])
          print('b' + base64.b32encode(b'\x01\x70' + mh).decode().lower().rstrip('='))
          PYEOF
          )
          if [ -z "$CIDV1" ]; then
            echo "::error::CIDv0 to CIDv1 conversion failed"
            exit 1
          fi
          echo "cidv1=$CIDV1" >> "$GITHUB_OUTPUT"

      - name: Update Cloudflare Worker KV Mainnet
        run: |
          curl --fail -X PUT \
            "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CF_ACCOUNT_ID }}/storage/kv/namespaces/${{ secrets.CF_KV_NAMESPACE_ID_MOC }}/values/current_cid" \
            -H "Authorization: Bearer ${{ secrets.CF_API_TOKEN }}" \
            -H "Content-Type: text/plain" \
            --data "${{ steps.upload.outputs.hash }}"

      # Create the release
      - name: Create GitHub Release
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          token: ${{ github.token }}
          tag_name: ${{ needs.bump_version.outputs.new_tag }}
          name: MoC Release ${{ needs.bump_version.outputs.new_tag }}
          body: |

            - CIDv0: `${{ steps.upload.outputs.hash }}`
            - CIDv1: `${{ steps.convert_cidv0.outputs.cidv1 }}`

            **MoC Mainnet gateways**:
            - Always last version available: [https://dapp.moneyonchain.com](https://dapp.moneyonchain.com)
            - https://dapp.moneyonchain.com/ipfs/${{ steps.upload.outputs.hash }}
            - https://gateway.pinata.cloud/ipfs/${{ steps.upload.outputs.hash }}
            - https://cloudflare-ipfs.com/ipfs/${{ steps.upload.outputs.hash }}
            - https://ipfs.io/ipfs/${{ steps.upload.outputs.hash }}
            - https://${{ steps.convert_cidv0.outputs.cidv1 }}.ipfs.dweb.link/
            - https://${{ steps.convert_cidv0.outputs.cidv1 }}.ipfs.cf-ipfs.com/
            - [ipfs://${{ steps.upload.outputs.hash }}/](ipfs://${{ steps.upload.outputs.hash }}/)

            ${{ needs.bump_version.outputs.changelog }}
